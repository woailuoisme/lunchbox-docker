# 错误页面服务配置
# 文件: traefik/config/error-pages.yml

# HTTP 服务配置
http:
  services:
    error-pages:
      loadBalancer:
        servers:
          - url: "http://error-pages:8080"
        passHostHeader: true

  routers:
    error-pages:
      entryPoints:
        - "websecure"
      priority: 30
      service: "error-pages"
      rule: 'Host(`error.{{ env "DOMAIN" }}`)'
      tls: {}
