# 通用服务器配置片段
# 包含所有服务器通用的基础配置

# HTTP/2支持
http2 on;

# SSL配置
include /etc/nginx/snippets/ssl.conf;

# 安全头部
include /etc/nginx/snippets/security-headers.conf;



# 日志格式（需要在各服务中指定具体的日志文件）
# access_log /var/log/nginx/service.access.log;
# error_log /var/log/nginx/service.error.log warn;